[project]
name = "psycopg2"
version = "2.9.9"
description = "Dummy psycopg2 to force binary installation"
requires-python = ">=3.10"
dependencies = [
    "psycopg2-binary==2.9.9",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
